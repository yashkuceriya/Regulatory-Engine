[phases.setup]
nixPkgs = ["python312"]

[phases.install]
cmds = ["python -m venv /app/.venv && . /app/.venv/bin/activate && pip install -r backend/requirements.txt"]

[start]
cmd = ". /app/.venv/bin/activate && uvicorn backend.main:app --host 0.0.0.0 --port ${PORT:-8000}"
